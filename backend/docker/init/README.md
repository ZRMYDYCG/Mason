Place an optional MySQL dump here if you want Docker to import existing data on first boot.
Run `pnpm prisma:migrate` and `pnpm prisma:seed` after the database starts to create Nest-specific tables and baseline data.
